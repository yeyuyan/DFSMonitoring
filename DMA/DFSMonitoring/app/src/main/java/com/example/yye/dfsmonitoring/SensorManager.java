package com.example.yye.dfsmonitoring;

/**
 * this class provides sensors' data to the detection Algorithm Controller to detect take-off and landing event
 */
public class SensorManager {
    private static final String TAG = "DFSSensManager";
    SensorManager(){
        //find the sensors connected to the phone
    }

    /**
     * get sensors' data
     */
    private void getSensorData(){
    }
}
